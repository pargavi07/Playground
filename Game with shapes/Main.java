#include<iostream>
using namespace std;
int main()
{
  int r,s;
  std :: cin>>r>>s;
  if(2*r<=s)
    std :: cout<<"circle can be inside a square";
  else
    std :: cout<<"circle cannot be inside a square";//Type your code here.
}